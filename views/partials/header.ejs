<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> | CinemaNuts</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
<header>
    <nav class="navbar">
        <div class="nav-left">
            <div class="logo">
                <a href="/">CinemaNuts</a>
            </div>
        </div>

        <div class="nav-right">
            <ul class="nav-links">
                <li>
                    <a href="/movies" class="<%= path === '/movies' ? 'active' : '' %>">Movies</a>
                </li>

                <!-- Members link (placeholder for now, can later point to a /members page) -->
                <li>
                    <a href="/movies" class="<%= path === '/members' ? 'active' : '' %>">Members</a>
                </li>

                <% if (isAuthenticated) { %>
                    <li>
                        <a href="/user/profile" class="<%= path === '/user/profile' ? 'active' : '' %>">
                            Profile
                        </a>
                    </li>
                    <li>
                        <a href="/auth/logout">Sign out</a>
                    </li>
                <% } else { %>
                    <li>
                        <a href="/auth/login" class="<%= path === '/auth/login' ? 'active' : '' %>">
                            Sign in
                        </a>
                    </li>
                    <li>
                        <a href="/auth/register" class="<%= path === '/auth/register' ? 'active' : '' %>">
                            Create account
                        </a>
                    </li>
                <% } %>
            </ul>

            <div class="header-search">
                <form action="/movies" method="GET">
                    <div class="input-group">
                        <input
                                type="text"
                                name="q"
                                placeholder="Search for movies..."
                                aria-label="Search movies"
                        >
                        <button type="submit">Go</button>
                    </div>
                </form>
            </div>
        </div>
    </nav>
</header>

<main>
